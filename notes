# Playtest notes / backlog

Open work only. When an item is done it **moves** to [`doc/dev/history.md`](doc/dev/history.md) — it does not get a checkmark and stay here, which is how this file grew to 33 completed entries and an item complaining about them.

Durable rationale ("why is the code like this?") belongs in [`doc/dev/decisions.md`](doc/dev/decisions.md); the chronological record, including approaches measured and reverted, is in `history.md`.

**Audited 2026-08-12** (the previous pass was 2026-08-10). Every open item making a checkable claim was re-verified by *running* something — a grep against `master`, a generated map, or the code itself — not by re-reading the item. That distinction is the point: several claims corrected below had been read past repeatedly, and three separate false claims were written into this file and into `decisions.md`/`history.md` in the two days before this audit.

**Corrected in this pass:**

- **Stage 6 was not open work.** It was built, measured at full protocol scale and reverted as a no-op — `decisions.md` had carried that verdict the whole time while this file described it as a stage still to build.
- **The lane hosts are not commented out.** `ssh-hosts.env` lists four, none commented; the MP-campaign item said the opposite. Heat is why that campaign is parked, not lanes. — **STALE WITHIN HOURS (noted 2026-08-15): `pc-tobi-laptop-guthand.lan` was commented out at 23:39 the same evening and still is.** Three lanes, not four. This line is left standing because it is what made the MP item's "worth investigating" bullet look credible for two days; probe the file, never a remembered count.
- **Player display names shipped** (2026-08-10, `getPlayerDisplayName` in `engine.ts`); the item still said no per-player identity concept existed. Removed.
- **`pickThreat` is at `combatPolicy.mjs:~980`**, not `~891` — the file grew today and every reference to that line was stale.
- **319 lines of finished multiplayer work (steps 1-7) moved to `history.md`**, where steps 8-11 already were. I moved 8-11 when they landed and left 1-7 behind, so the backlog carried them for three weeks.

**Verified still true**, so they need no re-checking: `pickThreat` filters `e.alive && e.aggroed`, and both gated and ranked on `Math.hypot` — confirming the walking-distance item, which was then *fixed* later the same day (see `history.md`); no threat give-up exists (reverted in `8390c8e`); the bot dispatches zero mouse events, so its aim is still keyboard-quantised; `VIRTUAL_STEP_MS` is still 50; `BOT_AIM_LEAD` is off (**no longer true — it ships ON gated at 2 tiles since 2026-08-14**) and `BOT_ANGULAR_FIRE_GATE` on; `BOT_WALKING_DISTANCE_BLOCKERS` defaults true; `DECORATIONS_ENABLED` is false; `hitFraction` is still `halfWidthPx / scatterPx`; `enemyAi.ts` still has exactly one `spawnProjectile` call site and no weapon concept; `HOST_NAV_DEADLINE_MS` and `perfDebug.ts` exist; the Firefox CI skip is still in place (4 sites).

Items recording a *measurement* rather than a code claim (the demo-campaign difficulty numbers, the damage-source breakdowns, the harness-specific wedge finding) were not re-run — they are history, and their dates say when they were true.

## Open

- [ ] **demo-campaign measured with a correct bot (2026-08-09): 14 of its 17 levels are free, and L15 is the entire campaign. Measurement only — no content changed.**
  - **SUPERSEDED 2026-08-12: the campaign no longer kills the bot at all, and L15 is not the wall any more.** Measured while A/B'ing the pellet-cone model at `LEVEL_LIMIT=17`: **160 attempts across 8 combos (4 profile x difficulty cells x 2 arms), zero failures**, with 19-20 of 20 reaching **both** L15 and L17 in every cell. Pro/hard specifically went from the 1-of-42 clearing L15 recorded above to **20 of 20**.
  - **What changed is the bot, not the content** — nothing in `demo-campaign/` moved between the two measurements, while the loot treadmill, the standoff, the Elite cap, the angular fire gate, the level-1 planner desync and the teleporter-pad give-up all did. The 2026-08-09 numbers are not wrong; they describe a bot that no longer exists.
  - **The consequence is bigger than this item.** The demo campaign is now exhausted as an A/B substrate: with a 100% clear rate and zero deaths, `qualifyRate`, the conditional death rate and attrition **cannot discriminate any bot change**, at `LEVEL_LIMIT=8` or 17. Yesterday I blamed the qualify-bar blind spot for that; the real reason is simply that there is nothing left to survive. **Any future bot A/B needs a staged real repo** (ripgrep/curl/serilog) rather than this campaign, or its survival half is decoration.
    - **RESOLVED 2026-08-13 — the substrate is staged curl, and the recipe is in `history.md`.** Piloted live rather than picked from the archive, because every per-repo number on record was stale: L1-L8 clear at 100%, **L9 at 45%, L10 at 33%**, nothing past L11. Use per-level clear rate at L9/L10 and levels-cleared-per-attempt; **campaign completion is 0% and must not be a metric** — it is saturated at the floor exactly as demo-campaign is at the ceiling. wolf3d also discriminates but costs ~7x (0.29 vs 1.96 attempts/min) and was dropped. Two cautions: re-staging curl does **not** reproduce the 2026-08-09 staging (different files at slots 7/10/12/14), and this harness produced a **20.4pp swing at n~20 from a lever measured flat**, so treat anything under ~25pp at that n as noise.
  - **One unexplained detail, recorded rather than rounded away**: two of the eight cells show `sampleCount` 19 rather than 20 from L15 on, with `failures=0` and no stuck or crash. A run stopped contributing snapshots without dying. Same shape as the single missing run seen on 2026-08-11. Not chased.
  - **Hard, 45 runs, bot behaving correctly** (the give-up A/B's OFF arm, which is what HEAD now does):

    ```
    L1-L9 45/45   L10 44/45   L11 43/44   L12 43/43   L13 43/43   L14 42/43
    L15 1/42      L16 1/1     L17 1/1
    ```

    Three deaths across levels 1-14 combined, then **L15 kills 41 of 42**. L16 and L17 clear whenever they are reached. So the campaign's difficulty is not spread thin — it is one level, and everything after it is a formality.
  - **This is the same shape the old bot showed** (2026-08-05: L1-14 flawless 16/16, L15 8/16, L17 0/8), so it is not an artefact of this bot. What changed is that L17 is now clearable — the Elite cap turned its 3 Elites from ~3,225 HP each into packs of ~500, and total room HP is essentially untouched (9,665 vs 9,675). The wall moved from "L15 *and* L17" to "L15 alone".
  - **The premise of the question holds, with a caveat.** The campaign was tuned against a bot with at least four defects, and `ELITE_HP_MULTIPLIER` was halved 4->2 specifically because the bot died 12/12 on level 12 — a fight it was knife-trading. But *this* measurement does not rest on that: 14 levels at ~99% clear on Hard is the finding, and it reproduces across two very different bots.
  - **What "harder" has to mean now.** Elites are capped, so a single huge function can no longer make a wall; the measured difficulty driver is exposure-time x enemy count (three A/Bs, total damage taken invariant at 122/127/126). Harder levels therefore mean source files with **more callable entities**, not one monster function. And L1-14 being gentle may be deliberate — this is the showcase campaign — so the case is strongest for the back half.
  - **Easy/normal numbers from the same session are CONTAMINATED** and must not be quoted: they come from the full 3-difficulty capture, which ran with the give-up active (reverted in `8390c8e` as a regression). Only the hard arm above is clean. Re-measure easy/normal before using them.
  - **The bot is not a difficulty oracle.** It is superhuman at some things and far worse at others — measured taking up to 53% of its damage while not fighting. "The bot clears it" is evidence the content is unchallenging *to this instrument*, not that a player will breeze it. What makes the case here is the margin (3 deaths in 14 levels x 45 runs), not the bare clear rate.
  - **Sequencing trap**: `defaultHighscore.ts` is generated from this campaign, so any content change invalidates it. Regenerate *after* deciding difficulty, not before — this session regenerated twice for exactly this reason.

- [ ] **The bot is destined to become the deathmatch opponent, which changes what "good enough" means for every open bot item (user, 2026-08-09).** Recorded because it reorders the backlog rather than adding to it.
  - **The bar changes.** As an *instrument* the bot only has to be unbiased and stable — it may play badly as long as it plays badly *consistently*, which is why several defects were tolerable. As an *opponent* it has to be good and fun, and "consistently mediocre" is the failure mode rather than the escape hatch. `multiplayer-research.md` already files "adapt balancing AI for deathmatch/bots" as its own initiative and warns it is not an adaptation of existing code; this is the reason to believe that.
  - **Which open items get promoted.** They stop being measurement noise and become gameplay bugs the player would *see*:
    - **Threats have no give-up** — an opponent that locks onto something it cannot reach and stops playing is unmissable in PvP.
    - **`pickThreat` uses `visible` mostly as a sort key** — an opponent tracking you through a wall reads as cheating, which is worse than reading as stupid. **Narrowed 2026-08-12, not closed**: an occluded enemy is now filtered out when it is far *by corridor* (the walking-distance fix), so the blatant case is gone, but one occluded a few tiles away through a thin wall is still a live target and still tracked through it.
    - **The wedges** (curl 2.0% of level-visits, improved but unfixed) — a bot that stops moving is the single most visible failure a deathmatch opponent can have.
    - **Route/replan churn**, still unresolved.
  - **Three design choices are instrument-shaped and would be wrong for an opponent.** Worth knowing before anyone treats the current bot as a starting point:
    1. **Keyboard-only input.** `dispatchSegment` sends synthetic `KeyboardEvent`s and deliberately never the mouse. Aim is therefore quantised to key-hold durations — today's turn-split got the *granularity* right, but the mechanism is still "hold a turn key for N ms", not "point here". A credible deathmatch opponent wants direct aim.
    2. **A 50ms decision window** (`VIRTUAL_STEP_MS`), i.e. 20 decisions/sec, and `MultiplayerBot` raises `minDecisionMs` further for lockstep. Fine for measurement, coarse for duelling.
    3. **The profiles are bot-shaped, not player-shaped.** Casual/Gamer/Pro exist to span a *measurement* range (`fireAngleEps`, `ammoThrift`, `selfHarmAversion`); as difficulty tiers a player picks, they would want re-deriving from "how does this feel to fight" rather than "how does this spread telemetry".
  - **Today's aim fix is on this path**, not a detour: `turnSplitIntent` is the first change made because the bot should *play* better rather than because a metric was contaminated.
  - **The related architectural item already exists** in this file — "decouple bot decisions from engine, for more performance ... maybe even thread the bot(s)" — and its "multiple bots" clause is exactly this. Do not scope deathmatch bots without it.

- [ ] **All three notions of "level 1" now agree — the solver third was ALREADY FIXED (2026-08-07), and this item claimed otherwise for four days. Only a drift risk is left.** Verified 2026-08-11 by three checks: `order()` picks `sorted.find(withMain) ?? sorted[0]` (`stage-campaign.mjs:262`), which is exactly `findEntrypointByScanning`'s `bestWithMain ?? bestOverall`; `complexityByFile` scores with the browser's own `complexityScore` and the zero-complexity filter uses that same number; and the `findEntrypointByName` stage is unreachable for a staged campaign *by construction*, since staging renames every file `${slot}_${path}` and `03_src_main.c` never equals `main.c`. The "models only the `bestOverall` branch" claim described the pre-2026-08-07 world; I repeated it without checking. The rest of the item has moved to `history.md`.
  - **The residual is a drift risk, not a bug.** The solver *re-derives* the rule where the planner now *asks* for it (`campaignLevelOrder`). It agrees today; it can silently stop agreeing if `findEntrypointByScanning` or the staging naming scheme changes, and nothing would fail. Making `order()` ask too would close that — worth doing only if the staging path misbehaves again.

- [ ] **Regular and Edge Case enemies do 75-100% of the damage and have no backlog item (2026-08-06).** Written in place of the refuted "level size is the real outlier" bullet. Across the 7-repo sweep, pooling raw `damageTaken.by`: Elites are **at most a quarter** of all damage a run takes (wolf3d 24.8%, codeenstein3d 21.3%, laravel 10.4%, curl 10.0%, ripgrep and the two zero-Elite repos 0%), while regular + Edge Case do the rest. Elites dominate only the *final* level (62-98%) — they are the full stop, not the attrition.
  - **ripgrep is the pure case**: 0% Elite damage, its single Elite never reached, and it still lost every one of 45 runs. An Elite clamp would do nothing for it.
  - **Deliberately not a proposed fix.** This is a hypothesis about where to look, not a measured problem with a known size — and per `remediation-order-2026-08-06.md` it should not be sized until the bot fixes land, since the current numbers are contaminated by the bot walking 14,000 tiles for loot and knife-trading Elites.
  - **What would size it**: after Stage C's re-measure, the share of deaths where no Elite was present at all, and the damage-per-enemy the attrition archetypes actually deliver. Review §10.
  - **SIZED 2026-08-13 on ~1,000 post-fix runs, and the hypothesis holds — but the interesting number is not the one this item asked for.** Full tables in `history.md`. Attrition archetypes do **88.4%** of enemy damage on curl (the only one of the three staged repos that contains Elites at all) and **100%** on serilog and ripgrep, which stage none. The Elite share on curl is **11.6%**, against **10.0%** in the pre-fix sweep above — so the contamination this item was waiting out barely moved this particular metric, and the hypothesis survives its own re-measure.
  - **Per-enemy, Elites are ~5x deadlier and still irrelevant**: curl damage-per-spawn is Elite **17.4**, normal **3.6**, Edge Case **0.7** — but 1,536 Elites against 46,309 normals and 49,151 Edge Cases. Individually lethal, numerically a rounding error. That is the mechanism behind "the full stop, not the attrition", now measured rather than inferred.
  - **The "deaths with no Elite present" metric turned out uninformative, and that is worth recording so nobody re-runs it.** On curl 99.5% of deaths happen on a level whose roster contains an Elite — but so does nearly every curl level, so the number measures staging, not lethality. Damage share is the discriminating quantity; roster presence is not.
  - **New and unasked-for: environmental damage varies enormously by repo** — 5.0% on curl, 7.6% on serilog, **29.7% on ripgrep** (mines 16.3% + acid 12.1%). That is a third of all damage taken coming from level furniture rather than enemies, on the repo whose Casual arm clears 3%. Anyone looking at ripgrep difficulty should start there rather than at its roster.

- [ ] **Bot mine retreat now backpedals instead of turning around — logic verified, real-world effect UNMEASURED (2026-07-31).** `mineRetreat` used to spin to face away from the mine while holding `KeyW`, so a retreat that began facing the mine (the normal case — you walk into one before noticing) *advanced on it* while turning: captured on `stage03_legacy_api.php` moving 7.86 -> 7.41, i.e. 0.45 tiles closer, mine 2.5 -> 2.2, over five decisions. That wasted motion is what made the retreat unable to finish: `findDangerousMine` fires at `MINE_BLAST_RADIUS + reactionBuffer` (2.4 + 0.32 = **2.72**) and `findDisarmableMine` only starts *above* it, but the bot never got past **2.6** before burning all `MINE_TARGET_GIVEUP_TICKS` (40 — exactly the count observed) and abandoning the mine. So it could neither pass nor shoot it. **The fix is the user's observation, not the original one:** the bot never needed to turn at all. `moveForward` takes a signed step (`forwardSign -= 1` for `KeyS`, engine.ts) so reversing is the same speed as advancing, sprint included — and backing up keeps the bot *facing* the mine, so the disarm shot is available the instant it clears the blast radius rather than requiring a second 180. **The bot had never dispatched `KeyS` anywhere in its history**; the whole backward axis was unused (it is Stage 7 of the rework plan, never built). `KeyS` also added to `TRANSLATING_KEYS` so a wedged backpedal is visible to `detectHeldKeyNoMovement`. **Caveat, stated plainly: unit-tested but never observed firing in play.** Across ~118,000 logged decisions in four runs (8 levels x 4, 3 levels x 3, 3 levels x 8), `mineRetreat` fired **zero** times, so `KeyS` never appeared. The original capture had 40 `mineRetreat` rows, so the branch does fire sometimes — the encounter is simply much rarer than that one capture suggested. Two earlier "it's fixed" readings in this session were invalid for exactly that reason: the post-fix runs had zero `mineRetreat` rows, so 0 oscillation findings measured the encounter not happening, not the fix working. **Do not A/B this until it has been seen firing** — the event is likely too rare to move any aggregate metric

- [ ] **MP campaign re-run parked 2026-07-30** (user's call — the machines are in their room and an hours-long run puts out too much heat; measured cost is ~13 min/attempt at 2p, i.e. ~28h single-lane for a Hard-only sweep). The 2026-07-24 Hard conclusions stay unverified against the fixed bot until it runs. **CORRECTED 2026-08-12: the lane-host half of this item was wrong.** It said "resume needs lanes restored" and that "`# mcdope@nas.lan` is commented out in `ssh-hosts.env`" — that file now lists **four** hosts and none of them is commented out (`mcdope@nas.lan`, `tobias@pc-tobi-laptop-guthand.lan`, `tobias@mcdope.org`, `tbaumer@46.224.76.98`). Whether each is reachable is a separate question this item cannot answer from the file alone, but "the lanes are commented out" is no longer a reason this is parked. Heat is.
  - **Reachability answered 2026-08-13, and the lanes work.** All four responded to ssh, and a 400-attempt capture ran clean across `local + nas.lan + mcdope.org + 46.224.76.98` — three lanes bootstrapped, ~2.5h of work in ~2.5h wall clock with every lane 66-100% busy, and **no orphaned dev servers left on any host** afterwards. One operational note for the next unattended run (**CORRECTED 2026-08-15**: the other one, "`pc-tobi-laptop-guthand.lan` is listed but the runner resolves only three hosts, worth investigating", was wrong — that host is **commented out** in `ssh-hosts.env` and has been since 2026-08-12 23:39, i.e. before this very capture, and `readHostList()` filters `#` lines. Three is the correct count, the laptop is deliberately off, and the audit line above claiming four uncommented hosts went stale within hours of being written. **Plan on three lanes plus local.**): lanes clone from origin and check out HEAD's *exact sha*, so **a capture on a staged campaign needs that commit pushed** — the run refuses lanes outright with "HEAD is not on any remote", which is a good guard but surprises you at launch time.

- [ ] **DELAYED UNTIL WE LEAVE BETA (user, 2026-08-10).** fog of war for minimap, same as automap (never applies to compass, always points to exit)
- [ ] **"decouple bot decisions from engine, for more performance. should also help with aim oszilattion etc. maybe even thread the bot(s)?"** — restated 2026-08-18, because two of its three clauses do not survive contact with the code. Nothing is being decoupled yet; the premise is being measured first.
  - **There is nothing bot-shaped in the engine to decouple.** The bot is entirely Node-side under `scripts/lib/`, reaching the page over Playwright/CDP. `src/` carries affordances (`__codeensteinTestHooks`, `?botRotSpeedMul=`, `BotPlayerState`) and zero decision logic.
  - **The real coupling is the virtual clock, and it is deliberate.** `virtualClock.mjs` monkeypatches `requestAnimationFrame` so the engine's frame queue never self-fires; it drains only inside `__pumpVirtualTime`, called only from `Bot#dispatchSegment`'s single `page.evaluate`. The engine therefore advances *synchronously inside the bot's dispatch call*, one CDP round trip per phase (two per combat decision, since `TURN_SPLIT_PHASES` is on). Simulated time being a pure function of the decision sequence — independent of machine load — is what makes every stored dataset comparable, and **any change here must preserve it**.
  - **"For more performance" is unmeasured, and the likely answer is not the bot at all.** No per-decision cost figure exists anywhere; `perfDebug.ts` has no bot phase. Meanwhile `advance()` is unconditionally `simulate(dt)` **+ `render()`**, so every one of the ~30k decisions in an attempt pays a full raycast frame nobody looks at — and `performance.md` puts a frame at ~7.7ms busy with ~93% in floor-cast + walls, against a localhost round trip of a fraction of that. **`?ablate=floor,effects,viewmodel,hud` already ships** (`resolveAblations`, `engine.ts:284`), deliberately un-DEV-gated. Test that before building anything on the round-trip premise. Not `sprites`/`walls`/`shade` — the `sprites` branch is what sets `this.target`, so ablating it stops the bot shooting.
  - **"Should also help with aim oscillation" is right for a reason the item does not give.** The atan2 branch-cut diagnosis was retracted and the detector was the bug (`history.md`). What survives are two measured step-size effects: `report-aim-error.mjs` names a structural 0-50ms observe->act latency as the driver of the archetype-split miss rate (Edge Case 48% / normal 75% / Elite 86%, flat across range), and the closed oscillation sweep left 80% ping-pong at a median 0.14 tiles from the nav target while one movement step is `ENGINE_MOVE_SPEED * stepMs` = 0.16 tiles. That sweep moved `ARRIVE_EPS` both ways and found it already in the trough of a steep U; **step size is the side of the ratio it never tried.**
  - **The A/B has a confound in two halves, and only one is reachable.** ~11 tick-denominated keys in `DEFAULT_TUNING` (`MAX_TICKS_PER_WAYPOINT`, `DOOR_OPEN_TICKS`, `MINE_TARGET_GIVEUP_TICKS`, the four stall/flip pairs, ...) plus **five module-level detector thresholds in `bot.mjs` that `CODEENSTEIN_TELEMETRY_TUNING` cannot reach at all** (`OSCILLATION_TICKS_THRESHOLD` 30, `STALL_TICKS_THRESHOLD` 20, `HELD_KEY_NO_MOVEMENT_TICKS_THRESHOLD` 10, `HP_DRAIN_FROZEN_TICKS_THRESHOLD` 2, `EXIT_FIGHT_CHUNK_TICKS` 10). Halve `stepMs` without rescaling both halves and the oscillation metric rises *mechanically* — the exact "the detector was wrong, not the bot" trap `decisions.md` already records. `COMBAT_TICK_BUDGET_MULTIPLIER` is dimensionless and must not be scaled.
  - **"Thread the bot(s)" only pays for multiple bots, i.e. deathmatch — and the harness path cannot ship.** `isTestHooksActive()` is `import.meta.env.DEV && ?testHooks=1` and Vite dead-code-eliminates the whole hook block from a production build, so an in-game opponent needs a real non-DEV API, not this one.
  - **MEASURED 2026-08-18 — full numbers in `history.md`.** `decide()` is **0.2-0.3%** of harness wall clock, so the item as literally worded is refuted. The costs are the raycast frame `advance()` renders per decision that nobody looks at (8.66ms/decision at c=12) and the CDP round trip (11.06ms). `?ablate=floor,effects,viewmodel,hud` already ships, removes 76% of the first, is byte-identical in telemetry at a pinned seed, and is worth **11.5%** end to end (196.7s -> 174.0s, 3 interleaved reps). Concurrency 8-20 is **flat within noise** while per-decision transport rises 2.5x — the transport is saturated, so no setting escapes the ceiling and the only lever left is **fewer round trips**. **The item's instinct was right and its reason was wrong: decouple the transport, not the decisions.** What remains open is whether a finer decision window is worth having (the aim-latency and step-granularity effects above) — the machinery for that A/B is built and inert (`TICK_BUDGET_SCALING`, `RECORD_STEP_MS`, `Bot#ticksScale`), the capture is not booked.
  - **Two reporting gaps found while scoping this, both verified by reading the code, neither fixed (2026-08-18).** They matter because they decide whether the A/B above can be *read*, not just run.
    - **`report-balancing-ab.mjs` must not be pointed at a capture directory.** `loadSide` merges a directory's `*.json` with a spread at the *difficulty* level, so across a chunked capture (`Casual-hard-001.json`, `-002`, ...) **the last file read silently replaces every earlier one**. No error, normal-looking output, computed from one chunk. It is built for the 4-combo telemetry protocol where one file is the whole side.
    - **HALF CLOSED 2026-08-18**: `npm run report:capture-survival <dir>` now pools a capture's chunks correctly and prints per-level reach rate and levels-reached-per-attempt. Demonstrated against the live capture: `loadSide` reported **5 attempts where there were 10** from only 3 chunks, and an arm finishes with ~12+. The event-log half (`report-balancing-events.mjs` not recursing into `events/<combo>-<seq>/`) is still open.
    - **Nothing committed produces the two metrics the recent A/Bs were judged on.** Per-level clear rate is *derivable* from `eventMetrics.levelPacing`'s `visits`/`outcomes`, and levels-cleared-per-attempt is not produced at all; `report-balancing-events.mjs`'s `collectLogPaths` reads only the top level of a directory, while a capture's logs live under `events/<combo>-<seq>/`, so pointing it at a capture root finds nothing. Every recent verdict on those numbers was computed ad hoc and then lost. A `--pool` flag with recursion would fix both and is the one genuinely missing instrument.
  - **Done as part of this item (2026-08-18): the bot and the engine now share their tile and sight predicates** rather than each carrying a copy — see `history.md`. That was the one part with an already-paid cost rather than a hypothesised benefit.
- [ ] objects on walls (properly sized, not simply "slap a texture on the whole wall")
    - [ ] flares/lamps with lightning and shadow effects
    - [ ] if room behind wall: window into next room (incl. LOS for enemy aggro - enemies should aggro when they see player through window)
    - [ ] posters (showing what? dev tools? images in the sourcetree?)
- [ ] Add three multiplayer coop demo campaigns
    - [ ] 1x finishable in 5-15min
    - [ ] 1x finishable in 10-30min
    - [ ] 1x finishable in 25-45min
    - [ ] should be visually different, each one should have its own set of levels. make sure each one has all features of our "code to level" pipeline.
- [ ] **Bot: what the rework did *not* fix.** Two structural faults survived, both because the obvious fix was measured and made things worse:
    - **The grid-locked gait.** `driveToward` still aims at BFS tile centres a tile apart, and a turn still truncates the step issued with it. Giving each key its own hold duration was the planned fix; it cost 25pp of qualify rate on Pro/hard and raised level times on all three combos, because a short decision during heading correction *is* the control loop. Any retry has to keep re-evaluation frequent while turning. Symptom to watch: headed pure-turn share, measured 30% -> 44% after sprinting landed.
    - **It still concedes the first shot** (`pickThreat` filters `.aggroed`). Planned as its own stage and not attempted — the plan rates it the riskiest remaining change, since aggro is sticky and waking enemies that would have stayed asleep can't be undone within a level.
  Optional and unattempted: sprint-backpedal retreats instead of turning 180° and running blind (touches `criticalHealth`/`mineRetreat`, the branches the 0%->72% diagonal-strafe regression came from).

- [ ] rooms need deco of some kind, currently an empty wasteland — implemented in Task 21 (server racks/plants/desks/code-blocks as billboard props) but felt annoying in playtest, so disabled again behind `DECORATIONS_ENABLED = false` in mapGenerator.ts. **Root cause confirmed by the user (2026-07-16): it's specifically the billboarding** — decor objects rotate to always face the camera, which reads as visibly wrong/artificial for boxy furniture-like shapes (a rack/desk/block spinning to track your POV as you walk past it), unlike round/symmetric things where billboarding is unnoticeable. Not a placement-density or art-style problem — the rendering *technique* itself is the issue. Code (generation + rendering) is intact; re-enabling needs a genuinely non-billboard rendering approach, not just a re-tune. Ignore for now (kept open, see the technical path below).
    - [ ] deco objects from wad? — investigated (2026-07-16, no code changed yet), then **revised** once the user confirmed the real root cause above was billboarding, not visual style/placement — this changes the answer meaningfully from the original pass.
      - **Billboard sprites (WAD or procedural) don't fix this at all** — swapping the procedural shapes for real WAD sprite art would still be billboarded (same `projectPoint`/always-face-camera rendering), so it would still spin to face the player exactly like today's boxes do. Real WAD sprite loading (as scoped in the "enemy textures from wad?" note above) is a legitimate, separate feature, but it is **not** an answer to this specific complaint — correcting my earlier note's framing here.
      - **The actual fix needs non-billboard geometry — real, orientation-fixed solid objects.** The raycaster already has exactly this: `placePillars` (`src/map/generation/props.ts`) places real 1×1 solid wall tiles (`grid[y][x] = 1`) to break up large rooms, rendered through the normal wall-rendering path — genuinely 3D, correct from every angle, no billboard rotation, because it's real raycaster geometry rather than a camera-facing sprite. The raycaster's wall renderer (`raycaster.ts`) already dispatches texture-per-tile-*kind* (plain wall vs `DOOR_TILE` vs `SECRET_WALL_TILE` vs `LORE_TILE` each get distinct textures) — a new tile kind per `DecorKind` (e.g. a `RACK_TILE`) would fit that exact same dispatch mechanism, textured distinctly per kind.
      - **This reframes the WAD question entirely, and in decor's favor**: solid-geometry decorations would use the *wall*-texture pipeline, not the sprite-patch pipeline — meaning they'd reuse the WAD loading/allowlist mechanism that already exists and already works today (`loadWad.ts`/`textureAllowlist.ts`, the same system real walls/doors/floors already use), not the net-new sprite-loading work the enemy-textures note scoped. WAD-sourcing solid decor textures is comparatively *cheap* once the non-billboard rendering itself is built — genuinely just another texture slot + allowlist, same shape as the existing wall/door/floor ones.
      - **Collision trade-off: resolved, not a concern (user, 2026-07-16)**: solid geometry means real collision (like pillars), diverging from the current system's "decorations never block anything" design goal — user confirmed this is fine ("if i run into a room, and there is a desk and i run into it, i am not surprised to be blocked :D"), i.e. real-world-furniture collision reads as expected/intuitive, not an annoyance. Clears the way to design this as solid, colliding geometry from the start (pillar-like placement via the existing `findPropSpot` logic) rather than needing a walkthrough/non-blocking variant.
- [ ] balancing
    - [ ] `npm run balancing:telemetry` (`scripts/run-balancing-telemetry.mjs`) now exists: a headless-Chromium bot plays `demo-campaign` with 3 profiles (Casual/Gamer/Pro) across all 3 difficulties (9 combos), collecting 3 qualifying runs each (a run "qualifies" once it clears level 3), and dumps a 7-category telemetry breakdown per level + campaign aggregate to `balancing_telemetry.json` (gitignored, regenerate on demand). Driven by new `window.__codeensteinTestHooks` hooks (`getTelemetrySnapshot()`, `getMines()`) and engine-side tracking in `src/engine/telemetry.ts`. A first round of balance tuning from this data has already shipped (loot drop-rate/amount reduction + a Toolchain miss-chance unlock, an Easy-difficulty damage increase, and worse enemy aim on Easy/Normal — see CHANGELOG's Unreleased section) — kept open for further iteration as more telemetry accumulates.
    - [ ] `npm run balancing:telemetry-multiplayer` (`scripts/run-balancing-telemetry-multiplayer.mjs`) enhancements — raised 2026-07-22 while a full default sweep (all player counts/difficulties/profiles + mixed combos) was live for several hours:
        - [ ] **Casual/Hard is close to a pointless combo.** Live data: `Casual/hard/2p` needed 30+ attempts to land just its 2nd qualifying run (a well under 10% qualifying rate) — the weakest profile at the hardest difficulty has a near-nonexistent real win chance, at least for a full/strict qualifying bar. Either exclude Casual from the Hard difficulty tier in the default matrix, or give it a much smaller qualifying target / explicit attempt cap so one unlucky combo can't blow up the whole sweep's wall-clock cost the way it did here.
        - [ ] **Qualifying bar could be more lenient than "fully clear the level."** This script already only drives one bundled level per run, not a full campaign (see its own top doc comment) — but if a future pass ever extends it toward multi-level/full-campaign coverage, don't require a full campaign clear to "qualify": mirror single-player's own per-profile qualify levels (`generate-default-highscore.mjs`'s `QUALIFY_LEVEL_INDEX_BY_PROFILE`, e.g. reaching level 4) rather than demanding full completion, especially given how brutal full clears are proving for weak-profile/hard-difficulty combos above.
- [ ] end2end/functional/visual tests (coverage target: all aspects of gameplay), and a dedicated audit confirming **no data leaves the app at all**. That claim used to read "besides highscores", which was forward-looking; public highscore boards were scoped and **scrapped** on 2026-08-19 (see [decisions.md](doc/dev/decisions.md#highscores-stay-on-the-device)), so the assertion is now the simpler and stronger one: `highscores.ts` is `localStorage` only and there is no outbound user data anywhere in the product. Assert it as a hard invariant — a `fetch` to anything but the signaling server (multiplayer only, and opt-in by joining) is a regression. The unit-test half of this item (100% coverage) shipped as Task 92 — see `## Done`. `scripts/verify-demo-campaign.mjs` (structural/map-generation coverage) and `scripts/verify-campaign-playthrough.mjs` (a real headless-Chromium save/highscore/replay mechanism test) are existing starting points to build the broader e2e/visual coverage on top of.
- [ ] User reports drastic framedrops when looking around or shooting on the nightmare github level suggestion (magento/magento2). I can't repro these locally. He uses Chrome on Win10 and the game usually runs at 120fps, but when shooting - even when out of ammo! - his fps drop to around 30. It's not an hw topic, since he has a Ryzen9, plenty of ram and a RTX3090. Confirmed still reproducing for this user even after the Task 82-85 engine perf pass (see the "code optimization/reuse" and magento entries in Done above) — that fix did not resolve it, reopened on purpose. Still unresolved/unrepro'd, but added opt-in diagnostics to capture from *his* machine next time it happens — see `src/engine/perfDebug.ts` (`?perfDebug=1`): per-frame phase-timing breakdown (input/sim/raycast/billboards+targeting/firing/particle-effects/hud), an "unaccounted" ms figure (raw frame time minus everything we measured — the tell for a GC/compositor/OS-level stall outside our own code), live entity counts, mousemove-event rate (tests a high-poll-rate-gaming-mouse-flooding-the-handler theory), Chrome heap usage, and audio shot-count/AudioContext state. Confirmed via code reading that the projection cost Tasks 82-85 removed doesn't run at all on an out-of-ammo `fire()` call (`ammo < ammoPerShot` returns before touching `projectLivingEnemies`/`audio.playShoot`), so whatever's causing the "even out of ammo" drop is something else this instrumentation should surface. Logs via `console.log` (NOT `console.debug` as previously noted here — corrected 2026-08-16; perfDebug.ts's own comment explains the lines are deliberately short and rate-limited because they DO mirror into the console sidebar). Next step once it fires again: send him a `?perfDebug=1` link, have him keep DevTools open with "Verbose" enabled, and get the captured log back. **2026-08 frame-budget audit context: his "120fps normally" means a 120Hz display — an 8.33ms frame budget, against which the pre-audit render busy time (~7.7ms, 93% of it the floor-cast + wall pass, measured scene-independent) leaves essentially zero margin; any GC/compositor/OS hiccup halves his rate to 60/30. That arithmetic explains "drops even when out of ammo" without any per-shot cost. Shipped: u32 floor-cast (-0.5ms). The real 120Hz enabler is the half-res floor option (-3.5ms, busy ~4.4ms = 53% of his budget), parked with the settings-tab item below. Not reproducible locally at 60Hz: quiescent captures show zero drops in every scenario incl. s4-magento-fire (perf_runs 2026-08-15/16); no 120Hz panel here to validate directly.**
- [ ] multiplayer support
    - [ ] adapt balancing AI for deathmatch mode / bots? host should define max players. for every missing player a random bot skill profile is launched. when a player wants to join: remove a bot. — still genuinely open/deferred: step 10 added real host-chosen `maxPlayers` (2-4) and real human guests filling those slots, but bot-filling of *empty* slots and any deathmatch (PvP, not coop) mode are both separate, unbuilt features.
- [ ] enemy textures from wad? if yes: what about hitbox? — investigated (2026-07-16, no code changed yet).
  - **Today: enemies are 100% procedural, zero WAD sprite loading exists.** `src/wad/loadWad.ts`'s `WadLoadResult` only ever loads wall/door/floor-style textures (`wallTexture`/`doorTexture`/`floorTexture`/etc.) — no sprite lump is read at all. Enemies are drawn in `src/engine/sprites.ts` as solid-color rectangular billboards (`enemyColor`/`enemyBodyColor`), sized by a single `sizeFactor` in `projectEnemy` (`ENEMY_SIZE`, scaled by `EDGE_CASE_SCALE`=0.55 or `ELITE_SCALE`).
  - **Loading real sprite art is feasible, same binary format already parsed**: Doom sprite lumps use the exact same "patch" pixel format as wall patches (`src/wad/patch.ts`'s `parsePatch` is already format-generic, not wall-specific) — no new binary parser needed. But the pipeline differs from walls: sprites are used as single patches per frame directly (no `compositeTexture.ts`-style multi-patch tiling/`TEXTURE1` step), and use Doom's frame+rotation lump-naming convention (e.g. `TROOA1`, `TROOA2A8` mirrored) that nothing in this codebase currently parses. This project's WAD-texture system also has a real security/curation stance worth respecting here, not shortcutting: `src/wad/textureAllowlist.ts` is a hand-curated, individually-IWAD-verified name list per slot (not "trust any name present") — real monster sprites (imp/zombieman/demon/etc.) would need the same treatment: a new, separately-verified allowlist, not a generic passthrough.
  - **The hitbox question is the real design fork, and it's grounded in something concrete**: today, hitbox and visual are *literally the same rectangle* by construction — `projectEnemy`'s one `sizeFactor`-derived projection is the exact object both `enemyBodyColor`'s fill (drawing) and `findTargetInProjections` (hit test, used by both ranged fire and melee's `meleeWouldHit`) operate on. Real Doom sprite art has irregular silhouettes with substantial transparent padding inside its bounding box — reusing today's full-rect hit test unchanged against real sprite pixels would make every enemy's hitbox visibly more generous than its drawn silhouette, a real fairness/feel regression, not just a cosmetic swap.
  - **Follow-up investigated (2026-07-16): what real silhouette-accurate hitboxes would actually take.** Turns out meaningfully cheaper than "pixel-alpha-accurate hit-test project" implies — the pieces already fit together well:
    - **The opacity data is already there for free.** A Doom patch's post format (`src/wad/patch.ts`) *is* its own alpha channel — each column's posts are exactly its runs of opaque pixels; any row not covered by a post in that column is transparent by definition. No separate alpha lookup or new data structure needed once a sprite's `Patch` is loaded — `patch.columns[x]` already answers "is this pixel opaque" directly.
    - **The engine's hit-test geometry is already a real 2D screen rect, not a raycaster-column hack.** `projectPoint` (`sprites.ts`) returns genuine `left/right/top/bottom` screen coordinates from one `size` value — `findTargetInProjections` checks a single `screenX` column against `cy` (the screen's fixed vertical center, since this engine has no mouselook pitch — `cy` is always `height/2`). That means mapping a hit-test point into the sprite's *native* pixel space is one clean linear interpolation per axis: `patchCol = floor((screenX - proj.left) / (proj.right - proj.left) * patch.width)`, `patchRow = floor((cy - proj.top) / (proj.bottom - proj.top) * patch.height)` — then look up `patch.columns[patchCol]` for a post covering `patchRow`. Elite/Edge-Case scaling doesn't complicate this at all: `proj`'s rect is already scaled, so the interpolation is unaffected by which scale factor produced it.
    - **The real new requirement is frame/rotation state, not the math.** Hit-testing needs the *exact same* patch (frame + rotation) that was actually drawn to the player that tick — today's system gets this for free by construction (draw and hit-test share the literal same rect object); a sprite-based version needs an explicit "current displayed patch" resolved once per enemy per tick and threaded to both the draw call and the hit test, or the two could silently diverge (a shot that visually missed the silhouette registering as a hit, or vice versa) — a real correctness requirement, not just an implementation detail.
    - **Blast radius**: `findTargetInProjections` is shared by ranged fire *and* melee's `meleeWouldHit` — a silhouette-accurate hit test would change melee timing/feel too unless deliberately scoped to ranged-only (an explicit decision to make, not an accident to discover later).
    - **Performance is a non-issue**: one column lookup + a short post-list scan per hit test (O(small) — a sprite has few posts per column) — negligible even for a multi-pellet shotgun blast checking several enemies at once.
  - **Revised recommendation**: given the above, real silhouette hitboxes are a legitimately achievable *second phase* once sprite loading itself exists (previous bullet) — not the much harder ask it first looked like. Still recommend shipping cosmetic-only sprites first (rectangular hitbox unchanged, zero risk to existing weapon-accuracy tuning) and treating silhouette-accurate hit-testing as a deliberate, separately-scoped follow-up, specifically because of the frame/rotation-sync requirement above — that's real new state to get right, worth its own focused pass and verification rather than bundling into the initial sprite-loading work.
  - **Real scope, if picked up**: new sprite-lump loading path in `loadWad.ts` (single-patch, no composite step), a new curated+IWAD-verified monster-name allowlist (same rigor as the existing wall-texture one), a decision on how many frames/rotations to support (Doom sprites can have up to 8 rotations plus walk/attack/death sequences — even a reduced subset is real per-enemy-kind work, not a one-line flip), and — if/when the silhouette-hitbox follow-up is picked up too — the frame-sync mechanism described above plus an explicit ranged-vs-melee scoping decision.
- [ ] **The shells/reload economy is unmeasured — a night run, parked by the user 2026-08-15.** Magazines and the shotgun's own `shells` pool shipped that day (see `history.md`); every number in them was sized off what the shared pool used to afford, not off a capture: `STARTING_SHELLS` 12, `SHELLS_DROP_AMOUNT` 2, `ELITE_SHELLS_DROP_AMOUNT` 8, the 70/30 bullets-vs-shells loot-weight split, and four `reloadSec` values.
  - **The cheap design does not exist, and that is the thing to know before planning one.** A single-arm run compared against the 2026-08-13 staged-curl baseline (L9 45%, L10 33%) is invalid: re-staging curl picks *different files at slots 7/10/12/14*, so the two runs are different campaigns. `history.md` already records that trap ("none of these numbers compare to the old 24/58").
  - **So it needs an A/B on one fresh staging**, which needs a tuning flag restoring the old economy — the repo's standing preference over a worktree baseline, which additionally cannot emit a metric newer than the baseline commit.
  - **The flag has a trap of its own**: implement it as a *runtime override* applied only when the env var is set. Adding fields to the `Weapon` table changes `SIMULATION_BALANCE`, whose hash covers `WEAPONS` wholesale — which would invalidate the `defaultHighscore.ts` board regenerated on 2026-08-15 and cost another regeneration.
  - **Sizing, from measured throughput.** The four-lane fleet (local + nas + mcdope.org + 46.224.76.98, all three ssh hosts probed alive 2026-08-15) ran **400 attempts in ~2.5h** on 2026-08-13, i.e. ~160/hour. Two arms at the settle protocol's n>=120 is 240 attempts (~1.5h); **add a third identical-code null-control arm** (360, ~2.2h) — the angular-gate lesson is that a single cell at p=0.043 was an unlucky draw, and only a null control measuring this harness's own 20.4pp noise floor at n~20 would have caught it early. A single 80-attempt arm (~30 min) answers only "is the bot starving on shells", which is worth running first.
  - Metrics are the ones staged curl is known to discriminate on: per-level clear rate at **L9/L10** and levels-cleared-per-attempt. **Campaign completion is 0% and must not be used** — it is saturated at the floor.
- [ ] **threaded / forked tests to speed them up? — the premise is already satisfied; what is left is elsewhere.** Checked 2026-08-19, because the item predates both halves being solved by other means.
  - **The unit suite is already forked and parallel.** `vitest.config.ts` sets no `pool` or `fileParallelism`, so vitest 4's defaults apply — forks, one worker per core (16 here). Measured: `vitest run --dir src` is **40.8s wall for 3,127 tests** against **69.8s** of summed worker time, and that gap *is* the parallelism. `--dir scripts` is 0.5s for 586. There is no win left to take here.
  - **The wall clock is the 16 browser-driven `verify-*.mjs` scripts**, which no vitest pool setting can reach — they are separate Node processes each driving a real Chromium/Firefox/WebKit. CI already parallelises them the only way available, by *job*: `verify-browser` is a 3-browser matrix, and `verify-replay` (45min timeout), `verify-multiplayer-transition` (20min), `verify-multiplayer-multiguest` and `verify-determinism` were each split into their own job on 2026-07-22 precisely so their multi-minute cost runs concurrently instead of stacking onto a shared chromium leg.
  - **What is actually left, in order of annoyance:**
    - **`npm run coverage` cannot be read locally while `balancing_corpus/` exists** (98MB, present after `npm run balancing:corpus`). Vitest's default glob picks up ~39 vendored QUnit/Django spec files and reports them as failures, so the gate is CI-only in practice and every local run needs `--dir src` instead. A `test.exclude` for the corpus closes it and is the single biggest local-DX win in this item.
    - **CI runs `npm run coverage || npm run coverage`** (`verify.yml:79`) — the whole ~81s suite twice on any flake. Find out what the retry was papering over before removing it; it may be load-bearing.
    - `verify:multiplayer-campaign` is deliberately *not* CI-wired (no bounded worst case) and is not a candidate.
  - **Before switching `pool` to `threads`**, note the suite constructs real `RaycasterEngine`s and jsdom canvases and relies on `restoreMocks` plus per-file module isolation, which `forks` gives for free — measure it, do not assume it is a drop-in.
- [ ] **Add more repo hosts beside GitHub — GitLab, Gitea/Forgejo, Bitbucket.** Narrowed 2026-08-19: the original wording was "should support any http(s) available git repo", and that half is **out of scope by decision** (see [decisions.md](doc/dev/decisions.md#repo-hosts-are-api-adapters-not-a-git-client)). This item is now only the API-backed hosts.
  - **The abstraction is already right, so this is genuinely small.** `RemoteFileHandle` (`workspace.ts:19`) is a *single* method — `getFile(): Promise<{text(): Promise<string>}>` — and everything downstream (file tree UI, `flattenParsableFiles`, entrypoint detection, level launching, replay) only reads `kind`/`name`/`path`/`children`. `github.ts` is 274 lines doing exactly two things: one recursive tree call (`/repos/{o}/{r}/git/trees/{branch}?recursive=1`) and lazy per-file text from `raw.githubusercontent.com`. **Nothing downstream changes.**
  - **Per host**: a sibling of `github.ts` (recursive tree endpoint + raw-file endpoint), a URL/shorthand parser (`parseGithubRepoInput` is github.com-only by regex), a `#tab-*` entry in `index.html`, and tests.
  - **Do not skip the rate-limit error path.** The GitHub one exists because unauthenticated is **60 req/hr**, and `raw.githubusercontent.com` does not CORS-expose its rate-limit headers — so a 403 there has to be *inferred* rather than read. Every host will need its own equivalent, and getting it wrong shows up as an unexplained "failed to fetch".
  - **Check CORS on the actual host before writing the adapter**, not after. It is the only property that decides whether a host is possible at all, and self-hosted Gitea/Forgejo instances configure it per-deployment — so "Gitea works" is a claim about an instance, not about Gitea.
  - The deeplink item below says "github/demo" and needs the same generalisation.

- [ ] **Self-updating multiplayer backend: `docker/update.sh --install` / `--uninstall` sets up a systemd timer.** Decided 2026-08-19 (user) after checking the alternatives — see [decisions.md](doc/dev/decisions.md#the-backend-updates-itself-by-pulling-ci-never-pushes-to-it).
  - **Nothing in CI changes, and no marker file or version endpoint is needed.** The git remote *is* the channel: the timer runs `update.sh`, which already `git pull`s and rebuilds. The original sketch's `.update-backend` + inotify cannot work (the frontend deploys by FTPS to a *webhost*; the backend is a different machine, no shared filesystem) and is dropped.
  - **The update logic already exists — do not reimplement it.** `update.sh` aborts on a dirty tree, detects the TURN profile from the running containers, pulls `coturn` only when relevant, rebuilds/recreates, prints status, warns on `docker/.env.example` drift. `--install`/`--uninstall` only need to write/remove a `.service` + `.timer` and reload; both must be idempotent.
  - **The one real wrinkle is sudo without a TTY.** `update.sh` deliberately runs as the *deploy user* and shells to `sudo` only for the docker calls, because a root `git pull` leaves the repo root-owned. Under systemd there is no TTY to prompt on, so this needs a `NOPASSWD` sudoers entry scoped to those compose commands. `--install` should write it (or verify it and fail loudly), because discovering it via a silently failing 04:00 timer is the worst version of finding out.
  - **Cadence: a quiet hour, not hourly.** Restarting signaling drops **open lobbies and pending joins** — `sessions` is an in-memory `Map` with a 5-minute TTL, so it is ephemeral by design, but someone sitting in a lobby gets kicked. Established peer connections survive, since peers talk directly once the handshake is done. So: `OnCalendar` at a quiet local hour, `RandomizedDelaySec`, and `Persistent=true` so a run missed while the host was off fires at next boot.
  - **Make failure visible.** A timer that silently stops is the classic form of this bug — the dirty-tree guard aborting loudly only helps if someone sees it. Add an `OnFailure=` unit, or at minimum have `--install` print the `systemctl status` / `journalctl -u` invocations and document them in `multiplayer-deployment.md` §6.6 next to the manual line this replaces.

- [ ] sounds/bgm (local bgm would override it) from wad?
- [ ] deathmatch multiplayer needs continous respawns of ammo, weapons, armor and health. also health and armor should only be collected till full, else it should remain on the ground for other players
- [ ] deeplink support for github/demo and file path
- [ ] add dedicated deathmatch demo campaign
- [ ] coop multiplayer should spawn additional boss enemies, requiring actual coop to kill. having a distinct color, roaming the whole map (not limitted to their room) and having more aggro, movement speed like normal enemies, should be visually larger as well. possible names: "VPN down", "CI failed", "Useless Meeting", "Git down", "CloudFlare", "AWS", "GCP", "Azure"
- [ ] rooms need more (and visually/behavirol diverse) enemies. raise hp per complexity and split into packs?
